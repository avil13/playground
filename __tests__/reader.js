const fs = require('fs');
const path = require('path');

function reader(dirPath) {
    const name = path.basename(dirPath);

    const codeLib = {
        source: null,
        testSrc: null,
        solution: null
    };

    // fs.re
}
